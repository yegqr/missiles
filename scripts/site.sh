#!/usr/bin/env bash
# Перебудова даних сайту. Прогноз тут не рахується — лише експорт того, що вже
# в базі: тривоги приходять кожні 5 хвилин, і сторінка має їх показувати, не
# чекаючи наступного дотренування.
set -euo pipefail
cd "$(dirname "$0")/.."
exec .venv/bin/python site/build.py
