#!/usr/bin/env bash
# Тривоги по місту. Найчастіший захід: саме тут «почалась / закінчилась».
# Реєстр КМДА щоразу віддає всю історію з точними часами, тому пропущений
# запуск нічого не втрачає — наступний добере навіть тривогу, що встигла
# початись і скінчитись між проходами.
source "$(dirname "${BASH_SOURCE[0]}")/_lib.sh"
lock alerts
FAILED=0
try kyiv_digital "$PY" ingest/kyiv_digital.py
exit $FAILED
